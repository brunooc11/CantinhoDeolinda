# Exemplo de ficheiro de configuração (não contém valores reais). 
# Copia este ficheiro para "config.env" e preenche os valores reais 
# (mantém o ficheiro real fora do Git e nunca o pushes). 
META_TOKEN= 
PHONE_NUMBER_ID= 
DESTINO=